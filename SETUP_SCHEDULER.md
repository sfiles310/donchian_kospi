# 윈도우 작업 스케줄러 설정 가이드

매일 장 마감 후 자동 실행되도록 등록하는 방법입니다.

---

## 1단계 — 배치 파일 준비

같은 폴더에 있는 `run_donchian.bat` 파일을 본인 환경에 맞게 수정합니다.

`run_donchian.bat` 내용:
```batch
@echo off
cd /d "C:\본인경로\donchian_kospi"
call C:\본인경로\venv\Scripts\activate.bat
python donchian_kospi_daily.py >> output\cron.log 2>&1
```

수정해야 할 부분:
- `C:\본인경로\donchian_kospi` → 스크립트가 있는 실제 폴더 경로
- `C:\본인경로\venv\Scripts\activate.bat` → 가상환경 활성화 경로
  (가상환경 안 쓴다면 `call` 줄 삭제하고 그냥 `python` 사용)

---

## 2단계 — 작업 스케줄러 등록

1. Win + R → `taskschd.msc` 입력 → 작업 스케줄러 실행
2. 우측 패널 → **기본 작업 만들기**
3. 이름: `KOSPI Donchian Daily Signal`
4. 트리거: **매일** 선택
5. 시작 시간: **오후 4:30** 권장 (한국 증시 마감 15:30 + 데이터 갱신 여유)
6. 동작: **프로그램 시작**
7. 프로그램/스크립트: `run_donchian.bat`의 전체 경로 입력
   ```
   예: C:\Users\본인\donchian_kospi\run_donchian.bat
   ```
8. 마침 → 등록 완료

---

## 3단계 — 추가 옵션 (권장)

작업 등록 후 해당 작업을 우클릭 → **속성** 열기

### "일반" 탭
- ☑️ **사용자가 로그온할 때만 실행** (기본값)
- 또는 PC가 항상 켜져 있다면: **사용자 로그온 여부와 관계없이 실행** + 비밀번호 입력

### "조건" 탭
- ☐ **컴퓨터가 AC 전원에 연결되어 있을 때만** (노트북이면 체크 해제 권장)
- ☑️ **이 작업을 실행하기 위해 컴퓨터를 절전 모드에서 해제**

### "설정" 탭
- ☑️ **요청 시 작업이 실행되도록 허용**
- ☑️ **예약된 시작 시간을 놓친 경우 가능한 한 빨리 작업 시작** ← 중요
  (PC가 4:30에 꺼져 있어도 켜진 직후 실행됨)

---

## 4단계 — 동작 확인

1. 작업 스케줄러에서 등록한 작업 우클릭 → **실행**
2. 결과:
   - 텔레그램에 메시지 도착 (신호 발생일 기준)
   - `output/dashboard.html` 갱신됨
   - `output/donchian_daily.log`에 실행 기록

---

## 매일 확인하는 흐름

```
1. 매일 오후 4:30 자동 실행
   ↓
2. 신호 발생 시 → 텔레그램 푸시
   ↓
3. 알림 받으면 → output/dashboard.html 더블클릭으로 상세 확인
   ↓
4. 다음 거래일 아침 → ACE 200 TR 매수 또는 매도 주문
```

---

## 문제 해결

| 증상 | 해결 |
|---|---|
| 작업이 실행 안 됨 | 작업 스케줄러 → 기록 활성화 → 오류 코드 확인 |
| Python을 못 찾음 | `python` 대신 `C:\Python312\python.exe` 같은 절대경로 사용 |
| 가상환경 활성화 실패 | 배치 파일에서 `call` 빼먹은 경우 많음. `call` 필수 |
| 한글 깨짐 | `chcp 65001` 줄을 배치 파일 맨 위에 추가 |
