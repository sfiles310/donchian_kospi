# Repository Rules

## 데이터 출처

- 외국인·기관 수급과 국내주식 시세는 KIS Open API(한국투자증권)를 통해서만 수집한다.
  엔드포인트 경로와 TR ID는 공식 예제 저장소에서 확인한 값만 `kis/endpoints.py`에 등록한다.
  https://github.com/koreainvestment/open-trading-api
- KRX 자료가 필요하면 공식 KRX Open API만 사용한다.
- `pykrx`, KRX 웹 엔드포인트 직접 호출, 스크래핑, 브라우저 자동화는 사용하지 않는다.

## 인증과 호출

- API 인증키는 환경변수(`KIS_APP_KEY`, `KIS_APP_SECRET`)로만 읽는다.
- 인증키, 앱 시크릿, 접근 토큰은 코드·로그·산출물·파일 어디에도 저장하거나 출력하지 않는다.
  토큰은 프로세스 메모리에만 두고, 수집이 끝나면 폐기한다.
- 조회 API만 사용한다. 주문·잔고 API는 `kis/endpoints.py`에 등록하지 않는다.
- 사용자의 명시적 확인 없이 실제 API 시험 호출이나 상태 확인 호출을 하지 않는다.
  `collect_kis_flow.py`는 기본이 모의 응답 모드이고, `--live`와 확인 절차를 거쳐야 실호출한다.

## 검증 원칙

- 개발과 검증은 먼저 모의 응답과 로컬 저장소로 수행하고, 실제 API 호출은 마지막에
  별도로 승인받아 실행한다.
- 모든 관측치는 그 값을 실제로 볼 수 있었던 시각(`data_available_at`)과 함께 저장한다.
  확정 전 값은 `is_provisional`로 표시하고 기본 조회에서 제외한다.
- 응답에 없는 필드를 0으로 채우지 않는다. 결측과 "수급 없음"은 다른 값이다.
